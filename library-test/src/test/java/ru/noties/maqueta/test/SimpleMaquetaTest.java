package ru.noties.maqueta.test;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.robolectric.RobolectricTestRunner;

@RunWith(RobolectricTestRunner.class)
public class SimpleMaquetaTest {

    @Test
    public void simple() {

        // todo|
        // check if sharedPreferences with `name` were requested
        // check default values are used (library defaults)
        // check user provided default values are used
        // check all generated methods do not throw
        // check builder is generated
        // check builder validate throws if argument is not provided
        // check that setter updates value in shared preferences
        // check that manual change to SharedPreferences reflects in getter
        // check observeWith (update is triggered)
        // check serializers are called
    }

}